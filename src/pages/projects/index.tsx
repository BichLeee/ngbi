import styled from "styled-components";

export const Projects = () => {
    return (
        <Container>
            <h1>Projects page</h1>
        </Container>
    );
};

const Container = styled.div`
    padding-top: 85px;
`;
